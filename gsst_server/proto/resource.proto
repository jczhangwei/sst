syntax = "proto3";
package sst_server.resource;

enum ResourceType {
    MATERIAL = 0;
    OPERATION_COUNT = 1;
    CURRENCY = 2;
}

enum ResourceDef {

}

message Resource {
    ResourceType type = 1;
    int32 id = 2;
    int64 num = 3;
}